# Rust
/target
**/target
Cargo.lock.orig

# Node
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Tauri
src-tauri/target/
src-tauri/gen/

# Secrets — never commit
.env
.env.local
.env.*.local
*.key

# Local model files (multi-GB, never committed)
models/
*.gguf
resources/llama-server.exe
resources/llama-server/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Test output
/test-output
*.jsonl.tmp

# Luna draft answers. They quote the machine's own history — window titles, document
# names and on-screen text — so they stay on the machine that produced them.
/drafts
